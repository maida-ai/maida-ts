# Upstream Maida contract snapshots

These files are exact test snapshots of the authoritative version and
behavioral contracts in `maida/contracts/`. Do not edit them independently.
Python remains the source of truth; TypeScript executes the vendored vectors to
prove compatibility.
