# normal

Deterministic happy-path trace for the current `spec_version: "0.2"` storage
contract. It contains one completed root span, one LLM call, one tool call,
token usage, tool args/results, and successful terminal metadata.
